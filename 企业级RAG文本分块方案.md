# 企业级 RAG 文本分块方案

## 全景架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        文档分块流水线                                    │
│                                                                         │
│  ┌─────────┐   ┌──────────┐   ┌──────────┐   ┌────────┐   ┌────────┐ │
│  │ 文档解析 │ → │ 结构识别  │ → │ 语义分块  │ → │ 上下文  │ → │ 向量化  │ │
│  │         │   │          │   │          │   │ 增强   │   │ 入库   │ │
│  └─────────┘   └──────────┘   └──────────┘   └────────┘   └────────┘ │
│                                                                         │
│  解析为纯文本    识别标题/段落     按语义边界切       补充上下文       多粒度存储  │
│  + 保留结构      /表格/代码块      不在句子中间断     防止信息孤岛     支持精确+模糊 │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 核心设计：多层级 Chunk 体系

```
┌─────────────────────────────────────────┐
│           Level 0: 原始文档              │  存储完整文档（不做 embed）
├─────────────────────────────────────────┤
│           Level 1: 大块 (Parent)         │  按章节/段落切，800-1500 token
│           用于最终交给 LLM 阅读           │  保留完整语义单元
├─────────────────────────────────────────┤
│           Level 2: 小块 (Child)          │  200-400 token，精细切
│           用于向量检索匹配               │  检索精度高
└─────────────────────────────────────────┘

检索流程：
  query → embed → 匹配 Child chunk → 找到其 Parent → 把 Parent 送给 LLM

为什么？
  小块 embed 匹配更精准（噪声少）
  大块送给 LLM 上下文更完整（不会断章取义）
```

---

## 每个 Chunk 的完整结构

```json
{
  "chunk_id": "doc_001_chunk_003",
  "content": "缓存命中率 43.2%，平均缓存条目 12,847 条...",
  
  "context_prefix": "[文档：星辰引擎 Q2 指标报告 | 章节：二、记忆晶体缓存层指标]",
  "content_with_context": "[文档：星辰引擎 Q2 指标报告 | 章节：二、记忆晶体缓存层指标]\n缓存命中率 43.2%...",
  
  "metadata": {
    "source_file": "星辰引擎内部指标报告-Q2.txt",
    "parent_chunk_id": "doc_001_parent_002",
    "chunk_index": 3,
    "total_chunks": 8,
    "heading_path": ["星辰检索引擎 Q2 报告", "二、记忆晶体缓存层指标"],
    "prev_chunk_id": "doc_001_chunk_002",
    "next_chunk_id": "doc_001_chunk_004"
  },

  "embedding": [0.012, -0.034, ...]  // 对 content_with_context 做 embed
}
```

关键点：**embed 的是带上下文前缀的文本**，而非裸内容。

---

## 分阶段实施

### 阶段一：上下文注入（1-2 天）

**改动最小，效果最明显。**

```python
# 分块后，给每个 chunk 注入来源上下文
for chunk in chunks:
    prefix = f"[文档：{filename}]"
    if chunk.metadata.get("heading"):
        prefix += f" [章节：{chunk.metadata['heading']}]"
    chunk.page_content = f"{prefix}\n{chunk.page_content}"
```

**解决的问题**：
- "星辰引擎"检索不到缓存相关 chunk
- chunk 脱离上下文后语义模糊

**验证方法**：

| 步骤 | 操作 |
|------|------|
| 1 | 准备 20 条测试 query（覆盖"按文档名找""按章节找""按具体内容找"） |
| 2 | 分别用旧方案和新方案入库同一批文档 |
| 3 | 对比 Top-5 召回命中率（人工标注哪些 query 应该命中哪些 chunk） |
| 4 | 预期提升：召回率 +10~15% |

---

### 阶段二：语义分块（3-5 天）

**替换固定字符数切割，按语义边界切。**

当前项目用 `RecursiveCharacterTextSplitter`（按字符数 + 分隔符切），问题是：
- 可能把一个完整段落从中间切断
- 表格、代码块被切碎

改为：

```python
# 方案 A：基于 Markdown 结构的分块（适合本项目）
# 按 ## 标题作为切割边界，每个 section 是一个 chunk
# 超长 section 再用 RecursiveCharacterTextSplitter 二次切

# 方案 B：语义分块（通用）
# 逐句计算相邻句子的 embedding 相似度
# 相似度骤降处 = 话题转换点 = 分块边界
#
# 句子1 ─ 0.92 ─ 句子2 ─ 0.89 ─ 句子3 ─ 0.41 ─ 句子4
#                                         ↑ 这里切
```

LangChain 已有实现：`SemanticChunker`（按 embedding 相似度断句）和 `MarkdownHeaderTextSplitter`（按标题层级）。

**验证方法**：

| 指标 | 怎么测 |
|------|--------|
| chunk 完整性 | 抽样 50 个 chunk，人工判断是否"读起来是完整的一段话" |
| 碎片化率 | 同一个 query 命中的 chunks 是否来自同一段落（应该是） |
| 召回率 | 同阶段一的 20 条 query 重新测，对比提升幅度 |

---

### 阶段三：Parent-Child 双层索引（5-7 天）

**架构性改动，效果最好但工作量最大。**

```
存储结构:
┌─────────────────────────────────────────┐
│  ChromaDB collection: rag_chunks        │  ← Child 小块，用于检索
│  ChromaDB collection: rag_parents       │  ← Parent 大块，用于送给 LLM
│  MySQL table: chunk_relations           │  ← 记录 parent-child 映射关系
└─────────────────────────────────────────┘

检索流程:
  query
    │
    ▼
  在 rag_chunks 中向量匹配 → 命中 child_chunk_007
    │
    ▼
  通过 chunk_relations 找到 parent_chunk_id → parent_003
    │
    ▼
  从 rag_parents 取出 parent_003 的完整内容 → 送给 LLM 生成回答
```

**核心逻辑**：

```python
# 入库时
parent_chunks = split_by_section(document, max_tokens=1200)  # 大块
for parent in parent_chunks:
    child_chunks = split_further(parent, max_tokens=300)      # 小块
    store_parent(parent)
    for child in child_chunks:
        child.metadata["parent_id"] = parent.id
        store_child(child)  # embed 并存入向量库

# 检索时
child_results = vector_search(query, collection="rag_chunks", top_k=5)
parent_ids = set(c.metadata["parent_id"] for c in child_results)
parent_docs = fetch_parents(parent_ids)  # 拿完整大块送给 LLM
```

**验证方法**：

| 维度 | 测量方式 |
|------|----------|
| 检索精度 | 同样的 query，对比 child 检索 vs 直接用大块检索的 Top-5 命中率 |
| 回答质量 | 准备 10 个需要上下文才能回答的问题（如"记忆晶体的 TTL 和命中率分别是多少"），对比有无 Parent 时 LLM 回答的完整度 |
| 延迟 | 增加一次 Parent 查询约 +5-10ms，可接受范围内 |

---

### 阶段四：智能 Overlap + 前后文链接（2-3 天）

```
chunk 1: [文档：xxx | 章节：yyy]
          ████████████████████
                        ████████████████████  ← chunk 2（overlap 20%）
                                      ████████████████████  ← chunk 3

+ 每个 chunk 存 prev_chunk_id / next_chunk_id
+ 检索命中后可选择性扩展相邻 chunk（滑动窗口）
```

**验证方法**：
- 测试"答案横跨两个 chunk 边界"的 query（如"缓存命中率和内存占用"分别在两个 chunk 里）
- 对比有无 overlap 时能否同时检索到两块

---

### 阶段五：文档类型感知分块（持续迭代）

不同类型文档用不同分块策略：

| 文档类型 | 分块策略 |
|----------|----------|
| Markdown/文档 | 按标题层级切，保留 heading path |
| 代码文件 | 按函数/类切，保留完整代码块 |
| 会议纪要 | 按议题/行动项切 |
| 表格类 | 每行或每组行作为一个 chunk，表头作为 context |
| PDF（扫描件） | 按页切 + OCR 后再语义分块 |

---

## 各阶段投入产出对比

```
效果提升
  ↑
  │                              ★ 阶段三 (Parent-Child)
  │                        ★ 阶段五 (类型感知)
  │                  ★ 阶段二 (语义分块)
  │            ★ 阶段四 (Overlap)
  │      ★ 阶段一 (上下文注入)
  │
  └──────────────────────────────────────→ 实施复杂度
```

| 阶段 | 性价比 | 建议 |
|------|--------|------|
| 阶段一 | ⭐⭐⭐⭐⭐ | **立即做**，改动 10 行代码，效果立竿见影 |
| 阶段二 | ⭐⭐⭐⭐ | 推荐做，LangChain 有现成工具 |
| 阶段三 | ⭐⭐⭐ | 效果最好但改动大，看业务需要 |
| 阶段四 | ⭐⭐⭐ | 和阶段二配合做，边际成本低 |
| 阶段五 | ⭐⭐ | 持续迭代，按实际文档类型逐个适配 |

---

## 通用评估框架（每个阶段都用）

```python
# 评估数据集格式
test_cases = [
    {
        "query": "记忆晶体的缓存命中率是多少",
        "expected_chunks": ["doc_001_chunk_003"],   # 应该命中哪些 chunk
        "expected_answer": "43.2%",                  # 最终答案应包含
    },
    ...
]

# 核心指标
Recall@K     = 命中的相关 chunk 数 / 总相关 chunk 数   （检索全不全）
Precision@K  = 命中的相关 chunk 数 / 返回的 chunk 数    （检索准不准）
Answer Score = LLM 最终回答是否包含 expected_answer     （端到端效果）
```

建议从 20-50 条手工标注的测试 query 起步，每个阶段跑一遍对比分数变化。

---

## 实施复盘：已完成 vs 计划中 vs 新增

### ✅ 已完成

| 项目 | 对应阶段 | 实测效果 |
|------|----------|----------|
| 上下文注入（文件名前缀） | 阶段一 | Recall 20% → 100% |
| 语义分块（embedding 相似度断句） | 阶段二 | 减少了句中断裂，但**表格/列表场景有副作用** |
| 文档类型感知（.md 按 ## 切，.txt 按段落切） | 阶段五 | 解决了标题/表格被切碎问题 |
| 标题粘连 + 短块合并 | 阶段二补丁 | 修复标题独立成 chunk 的碎片问题 |
| 自动化评估脚本 | 通用评估框架 | 10 条 test case，输出 Recall/Precision |

### 📋 计划中未做

| 项目 | 对应阶段 | 状态 |
|------|----------|------|
| Parent-Child 双层索引 | 阶段三 | 架构改动大，待业务需要时实施 |
| 智能 Overlap + 前后文链接 | 阶段四 | 可与阶段三配合 |
| 章节标题注入 context_prefix | 阶段一（完整模式） | 当前只注入了文件名，未注入 heading path |

### 🆕 新增补充（原计划未覆盖）

以下是实践中发现的关键经验和业界方案，补充进路线图：

---

## 阶段六：多级容错流水线（不依赖分块完美）

**核心认知：没有完美分块，用下游流水线弥补分块缺陷。**

当前项目已有的容错层：
- ✅ 多路召回（向量 + BM25 混合检索）
- ✅ Reranker 精排（BGE cross-encoder 打低分淘汰噪声）
- ✅ HyDE 查询膨胀（弥补短 query 语义不足）

可追加的容错层：

| 容错手段 | 做法 | 解决什么 |
|----------|------|----------|
| **检索后上下文扩展** | 命中 chunk 后，自动拉取前后相邻 chunk 拼接送给 LLM | 答案横跨 chunk 边界 |
| **多粒度冗余存储** | 同一文档存 2 份：原始大块 + 精细小块，检索小块，返回大块 | 兼顾检索精度和上下文完整 |
| **LLM 容错能力** | 给 LLM 5 个 chunk（含 1-2 个不相关），它自己能忽略噪声 | 减少对 Precision 的苛求 |

**验证方法**：构造"答案跨 2 个 chunk"的测试 query，对比有无上下文扩展时 LLM 回答质量。

---

## 阶段七：数据质量前置（入库前清洗）

**在分块之前解决问题，比在分块时解决容易得多。**

| 清洗步骤 | 做法 | 收益 |
|----------|------|------|
| 格式标准化 | 入库前用 LLM 或规则将文档统一为标准 Markdown（补充缺失的标题层级） | 让按标题切的策略普遍适用 |
| 表格归一化 | Markdown 表格转为"每行一条"的结构化文本 | 避免表格行被合成巨块或切碎 |
| 去噪 | 去掉页眉页脚、目录、冗余空行 | 减少无意义 chunk |
| 元数据补充 | 从文件名/首页提取：作者、日期、类别，写入 metadata | 支持按 metadata 过滤检索 |

**验证方法**：对比清洗前后同一批文档的分块质量（人工抽样 + 自动评估分数）。

---

## 阶段八：LLM 辅助分块（Contextual Retrieval）

**最前沿方案：让 LLM 为每个 chunk 生成上下文摘要。**

来自 Anthropic 的 Contextual Retrieval 方案：

```python
# 入库时，对每个 chunk 调一次 LLM
for chunk in chunks:
    context = llm.generate(
        f"以下是文档《{filename}》中的一个片段。"
        f"请用 1-2 句话概括这个片段在整篇文档中的位置和主题：\n\n{chunk}"
    )
    # 拼接上下文摘要作为 embedding 输入
    chunk.page_content = f"{context}\n{chunk.page_content}"
```

效果：比规则注入的 `[文档：xxx]` 前缀更智能——LLM 能理解"这是Q2报告中关于缓存层的性能数据"，而非机械的文件名。

| 优点 | 缺点 |
|------|------|
| 效果远超规则方案 | 入库成本高（每 chunk 一次 LLM 调用） |
| 通用，不依赖文档格式 | 延迟：1000 个 chunk 需几分钟处理 |
| 完全解决"术语表中时光回溯检索不到"的问题 | 适合入库量小但质量要求高的场景 |

**验证方法**：选 50 个 chunk，对比规则前缀 vs LLM 摘要前缀的检索 Recall。

---

## 阶段九：持续评估 + 反馈闭环

**不是一次性工程，而是持续运营。**

```
用户提问 → RAG 回答
    │
    ├── 用户点"有帮助" → 记录（query, 命中chunk）为正样本
    ├── 用户点"没帮助" → 追溯：
    │       │
    │       ├── 检索到了但答案不对 → Reranker/Prompt 问题
    │       └── 根本没检索到 → 分块问题 → 标注为分块缺陷
    │
    ▼
  定期回归：用积累的标注数据重跑评估，持续优化分块策略
```

| 建设项 | 做法 |
|--------|------|
| 评估数据集扩充 | 从 10 条 → 50 条 → 200 条，覆盖更多 edge case |
| A/B 测试能力 | 同一文档用两种分块策略入库到不同 collection，对比同一组 query 的分数 |
| 分块质量仪表盘 | 定期输出：平均 chunk 长度、碎片化率、空 chunk 率 |

---

## 更新后的全景路线图

```
性价比
  ↑
  │ ★ 阶段一 (上下文注入)                    ← ✅ 已完成
  │ ★ 阶段六 (多级容错)                      ← 部分已有(BM25+Reranker)
  │ ★ 阶段七 (数据清洗)                      ← 🆕
  │ ★ 阶段五 (类型感知)                      ← ✅ 已完成(.md/.txt)
  │ ★ 阶段二 (语义分块)                      ← ✅ 已完成
  │ ★ 阶段四 (Overlap)                       ← 📋 计划中
  │ ★ 阶段三 (Parent-Child)                  ← 📋 计划中
  │ ★ 阶段八 (LLM 辅助分块)                  ← 🆕 前沿
  │ ★ 阶段九 (评估闭环)                      ← 🆕 持续运营
  └──────────────────────────────────────→ 实施复杂度
```

### 下一步建议优先级

| 优先级 | 项目 | 理由 |
|--------|------|------|
| P0 | 阶段六：检索后上下文扩展 | 改动小（检索后取前后 chunk），立即弥补分块不完美 |
| P1 | 阶段七：txt 格式标准化 | 把 txt 在入库前转为 Markdown，统一走 ## 切割 |
| P2 | 阶段三：Parent-Child | 架构性提升，彻底解决"检索精度 vs 上下文完整"的矛盾 |
| P3 | 阶段八：LLM 辅助 | 入库量不大时性价比高，效果天花板最高 |
